            case 'pingme':
                if(args.length == 2){
                    x = 0;
                    while(x<args[1]){
                        msg.reply();
                        x++;
                    }
                }else{
                    msg.reply("must add number")
                }


                            break;
            case 'addstreamer':
                if(args.length == 3 && !(args[2] in streamers)){
                    streamers.content += ' ' + args[1] + ' ';
                    streamers.content += args[2] + ',';
                    msg.channel.send(streamers.content);
                }else{
                    if(args[2] in streamers){
                        msg.reply('Streamer is already in list!')
                    }else{
                        msg.reply('must add a username and url!');
                    }
                }
            break;
